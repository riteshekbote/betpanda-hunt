
===== TRIAGE 2026-09-15 11:18:16 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
I see the leads section is empty. Let me check the repository for context about the BetPanda program.
[0m→ [0mRead probe-results.md
[0m→ [0mRead leads
[0m→ [0mRead scope.yml
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead triage-output.txt
[0m→ [0mRead probe-results.txt
[0m→ [0mRead new-leads.txt
[0m→ [0mRead leads/lead-nemotron3.md[90m [offset=381][0m
Now I have a full picture. Here is the triage across all 7 leads identified across the hunt bot outputs:

---

## LEAD 1: Wildcard CORS + Credentials on `affiliates.betpanda.io/rest/*`

| Gate | Answer |
|------|--------|
| Q1 Scope | Yes — "all company-owned infrastructure… domains operated by BetPanda" in scope.yml |
| Q2 Reachable | Yes — public REST API, reflects any Origin in OPTIONS (confirmed by probe) |
| Q3 Impact | Yes — 20+ authenticated endpoints (players, transactions, password/reset, set-2fa-setting, payout-config) readable/writable cross-origin via credentialed fetch → PII exfil + full ATO chain |
| Q4 Provable | Yes — `OPTIONS -H "Origin: https://evil.com"` returns `ACAO: <origin> + ACAC: true` (passive). Full POC needs auth session (auth-helped) |
| Q5 Novel | Yes — no evidence of prior report; endpoints enumerated from JS bundle |
| Q6 Not always-rejected | Correct — this is not on the always-rejected list |
| Q7 Reasonable triager | Yes — wildcard CORS + credentials on a financial affiliate platform is a classic accepted finding |

**Verdict: VALID**

**Proof steps:** `curl -sS -D- -X OPTIONS https://affiliates.betpanda.io/rest/user/players -H "Origin: https://evil.com" -H "Access-Control-Request-Method: POST" -H "Access-Control-Request-Headers: content-type"` — confirm `Access-Control-Allow-Origin: https://evil.com` + `Access-Control-Allow-Credentials: true`. Auth-helped: capture session cookies from login, then `GET https://affiliates.betpanda.io/rest/user/players -H "Origin: https://evil.com" -H "Cookie: <session>"` and verify PII in response.

**Impact:** Cross-origin read/write of player PII, transactions, commissions, password reset, 2FA config on a real-money gambling affiliate platform. Full ATO.

**CVSS 3.1:** 9.1 (Critical) — AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N

**Reporting channel:** bugs.olivermaicher.eu

---

## LEAD 2: BOLA/IDOR on `betpandacasino.io/rest/user/*` money-flow endpoints

| Gate | Answer |
|------|--------|
| Q1 Scope | Yes — company-owned gambling platform |
| Q2 Reachable | Yes — Spring Boot backend behind Cognito JWT; endpoints exist (405 balances, 401 settings, 403 authenticate) |
| Q3 Impact | Yes — cross-user wallet/balance/bonus disclosure on real-money platform |
| Q4 Provable | Partial — endpoints confirmed to exist, but proof requires 2 test accounts + JWT manipulation (auth-helped, invasive) |
| Q5 Novel | Likely — no evidence of prior report |
| Q6 Not always-rejected | Correct |
| Q7 Reasonable triager | Yes, but only with authenticated POC |

**Verdict: HOLD** — Needs 2 test accounts to prove cross-user data access. Server-side ACL enforcement unknown. Cannot close without auth-helped POC.

---

## LEAD 3: Unauthenticated Event Injection on `cable.betpanda.io/cable/user-event`

| Gate | Answer |
|------|--------|
| Q1 Scope | Yes — company-owned subdomain |
| Q2 Reachable | Yes — public, unauthenticated POST accepted |
| Q3 Impact | Unclear — ingestion confirmed ("processed and saved") but no evidence of stored data rendered to users or secondary impact |
| Q4 Provable | Partial — POST returns 200 but no observable server-side effect (no stored XSS confirmed, no business logic trigger) |
| Q5 Novel | Yes |
| Q6 Not always-rejected | Correct |
| Q7 Reasonable triager | Borderline — accepts arbitrary input but no demonstrated impact beyond injection surface |

**Verdict: HOLD** — Impact unconfirmed. Needs evidence that stored data is reflected to other users (admin dashboard, reporting UI) or triggers business logic. Without that, this is an injection surface without demonstrated exploitability.

---

## LEAD 4: OAuth redirect_uri Validation Bypass on `betpanda.io/api/auth/authorize`

| Gate | Answer |
|------|--------|
| Q1 Scope | Yes |
| Q2 Reachable | N/A |
| Q3 Impact | N/A |
| Q4 Provable | N/A |
| Q5 Novel | N/A |
| Q6 Not always-rejected | N/A |
| Q7 Reasonable triager | N/A |

**Verdict: INVALID** — Confirmed false positive. `betpanda.io/api/auth/authorize` 301-redirects to `betpandacasino.io/api/auth/authorize` which returns SPA HTML (catch-all route). No server-side OAuth endpoint exists at this path. All `/api/*` paths on betpandacasino.io are SPA catch-all. Prior 200 responses were SPA HTML, not an OAuth flow.

---

## LEAD 5: IDOR on Affiliate Player Lookup by UID (`affiliates.betpanda.io/rest/player/uid/{id}`)

| Gate | Answer |
|------|--------|
| Q1 Scope | Yes |
| Q2 Reachable | Yes — endpoint exists (HTTP 400 confirms routing) |
| Q3 Impact | Yes — cross-tenant player PII if server doesn't enforce per-affiliate ACL |
| Q4 Provable | No — merged into Lead 1's CORS finding; same endpoint, same auth model. Requires auth session + 2 affiliate accounts |
| Q5 Novel | Merged — not independently novel |
| Q6 Not always-rejected | Correct |
| Q7 Reasonable triager | Merged into Lead 1 |

**Verdict: INVALID** — Subsumed by Lead 1 (CORS misconfig). The IDOR on this endpoint is the *exploitation path* for the CORS vulnerability, not a separate finding. Report as part of Lead 1.

---

## LEAD 6: Strapi CMS baseURL Override via localStorage (`affiliates.betpanda.io`)

| Gate | Answer |
|------|--------|
| Q1 Scope | Yes |
| Q2 Reachable | Yes |
| Q3 Impact | Conditional — requires prior XSS or subdomain compromise to set localStorage value |
| Q4 Provable | No standalone proof — requires chaining from another vulnerability |
| Q5 Novel | Yes |
| Q6 Not always-rejected | Correct |
| Q7 Reasonable triager | No — standalone impact is content injection on affiliate pages only (no PII/player data). Requires XSS to exploit. |

**Verdict: INVALID** — Parked by the hunt bot itself. Standalone impact is medium at best (CMS content manipulation). Requires prior XSS to set localStorage — not independently reportable.

---

## LEAD 7: k8s Dashboard / S3 Bucket / Feature Flags (misc.)

| Gate | Answer |
|------|--------|
| Q1 Scope | Yes |
| Q2 Reachable | No — dashboard.betpanda.io times out (internal ALB), S3 returns 403, flags.betpanda.io CF-challenged |
| Q3 Impact | N/A |
| Q4 Provable | No — cannot access any of these |
| Q5 Novel | N/A |
| Q6 Not always-rejected | N/A |
| Q7 Reasonable triager | N/A |

**Verdict: INVALID** — All three are either unreachable (dashboard timeouts), access-denied (S3 listing disabled), or blocked by Cloudflare (flags). No evidence of exposure.

---

## Summary

| # | Finding | Verdict | CVSS | Reportable |
|---|---------|---------|------|------------|
| 1 | Wildcard CORS + credentials on affiliates.betpanda.io REST API | **VALID** | 9.1 Critical | Yes |
| 2 | BOLA/IDOR on betpandacasino.io money-flow endpoints | **HOLD** | TBD | Needs 2 test accounts |
| 3 | Unauthenticated event injection on cable.betpanda.io | **HOLD** | TBD | Needs stored data / secondary impact proof |
| 4 | OAuth redirect_uri bypass on betpanda.io | **INVALID** | — | False positive (SPA catch-all) |
| 5 | IDOR on affiliate player lookup by UID | **INVALID** | — | Subsumed by #1 |
| 6 | Strapi CMS baseURL override | **INVALID** | — | Requires prior XSS, not standalone |
| 7 | k8s dashboard / S3 / feature flags | **INVALID** | — | Unreachable / access-denied |

**Actionable report:** Lead 1 (CORS) is ready to submit to bugs.olivermaicher.eu with passive OPTIONS proof + auth-helped POC once affiliate session cookies are captured.
