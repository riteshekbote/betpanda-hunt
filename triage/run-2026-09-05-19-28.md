
===== TRIAGE 2026-09-05 19:28:39 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*probe*"[90m 2 matches[0m
[0m✱ [0mGlob "**/*results*"[90m 2 matches[0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m→ [0mRead probe-results.md
[0m→ [0mRead new-leads.txt
[0m→ [0mRead .
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory
[0m→ [0mRead knowledge
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead probe-results.txt
[0m→ [0mRead inventory/betpanda.md
[0m→ [0mRead knowledge/index.md
Let me read the remaining lead files and the full probe results to get all the leads.
Now I have all the context. Let me produce the triage for every distinct hypothesis the bots surfaced.

---

## TRIAGE — 7-Question Gate

### LEAD 1: Wildcard CORS + Credentials on `affiliates.betpanda.io/rest/*`

| Q | Answer |
|---|--------|
| Q1 Scope | YES — scope.yml covers "all company-owned infrastructure, APIs"; `affiliates.betpanda.io` is a discovered dedicated host. |
| Q2 Reachable | YES — HTTP 200, public REST API, no Cloudflare challenge. |
| Q3 Impact | YES — `access-control-allow-origin: <any> + access-control-allow-credentials: true` confirmed via OPTIONS probe; 20+ authenticated endpoints (players, transactions, password/reset, set-2fa-setting, agent/set-deposit-withdraw-limit) allow cross-origin credentialed read/write → player PII exfil, financial tampering, ATO. |
| Q4 Provable | PARTIALLY — CORS misconfiguration (ACAO reflect + ACAC:true) is passively provable via `curl -D- -H "Origin: https://evil.com"` OPTIONS. Full impact proof requires an authenticated affiliate session (AUTH_HELPED). |
| Q5 Novel | UNKNOWN — cannot confirm unreported without program history, but no evidence of prior report. |
| Q6 Not always-rejected | YES — not DoS, not enumeration, not clickjacking, not email config, not brute-force, not self-XSS. |
| Q7 Reasonable triager | YES — CORS + credentials with financial endpoints on a gambling platform is a well-understood critical class. |

**Verdict: HOLD**
Reason: CORS misconfiguration confirmed passively but full impact POC requires authenticated affiliate session. Report once auth-assisted proof demonstrates cross-origin data exfil.

**Minimal read-only proof steps (partial):**
1. `curl -sS -D- -X OPTIONS "https://affiliates.betpanda.io/rest/user/players" -H "Origin: https://evil.com" -H "Access-Control-Request-Method: POST" -H "Access-Control-Request-Headers: content-type"` — confirm ACAO reflects evil.com + ACAC:true.
2. Confirm `/rest/user` returns 401 unauth (cookie-gated auth model).

**Impact:** Critical — cross-origin player PII/transaction exfil, password reset, 2FA config, deposit/withdrawal limit tampering on gambling affiliate platform.
**CVSS 3.1:** 9.1 (AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) — assumes SameSite cookie allows credentialed cross-origin; downgrade if SameSite=Strict.
**Reporting channel:** bugs.olivermaicher.eu

---

### LEAD 2: BOLA/IDOR on `betpandacasino.io/rest/user/*` money-flow endpoints

| Q | Answer |
|---|--------|
| Q1 Scope | YES — betpandacasino.io is company-owned gambling platform, /rest/user/* endpoints are confirmed real Spring Boot routes. |
| Q2 Reachable | YES — /rest/user/account-balances-and-bonuses (405 GET = exists), /rest/user/settings (401 auth-gated), /rest/user/authenticate (403 JSON with CAPTCHA requirement). Real authenticated endpoints. |
| Q3 Impact | YES (potential) — money-flow endpoints (balances, wallet, bets) are classic BOLA targets. Same-origin JWT manipulation could expose cross-user financial data. |
| Q4 Provable | NO (current state) — requires 2 authenticated accounts to demonstrate cross-tenant access. No passive proof possible. CORS is properly pinned (no cross-origin vector). |
| Q5 Novel | UNKNOWN — but money-flow IDOR on gambling platforms is well-studied class. |
| Q6 Not always-rejected | YES — IDOR/BOLA on financial endpoints is not on always-rejected list. |
| Q7 Reasonable triager | PARTIALLY — endpoint existence confirmed but no evidence of actual authorization bypass. Speculative without auth-assisted testing. |

**Verdict: HOLD**
Reason: Hypothesis is high-value but zero passive proof of authorization bypass exists. Only HTTP status codes (401/405) confirm endpoint existence, not IDOR.

**Minimal read-only proof steps (when auth-assisted):**
1. Create 2 test accounts on betpandacasino.io.
2. Capture JWT from account A; POST to `/rest/user/account-balances-and-bonuses` with account A's JWT referencing account B's identifier.
3. Compare response to account A's own data.

**Impact:** Critical — cross-user wallet/balance/bonus disclosure/tampering on real-money gambling.
**CVSS 3.1:** 8.8 (AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) — requires authenticated session.
**Reporting channel:** bugs.olivermaicher.eu

---

### LEAD 3: Unauthenticated Event Injection on `cable.betpanda.io/cable/user-event`

| Q | Answer |
|---|--------|
| Q1 Scope | YES — cable.betpanda.io is dedicated company-owned host. |
| Q2 Reachable | YES — HTTP 200, POST accepts arbitrary JSON, no auth required, CORS *. |
| Q3 Impact | PARTIALLY — confirms ingestion with "processed and saved", but no evidence of output/reflect to other users (stored XSS) or backend injection. Analytics data poisoning at minimum. |
| Q4 Provable | PARTIALLY — POST accepts arbitrary JSON with 200 response (passive proof of ingestion). But impact (stored XSS, injection) requires observing server-side rendering in admin dashboards (not achievable passively). |
| Q5 Novel | UNKNOWN — unauthenticated event ingestion is a known pattern. |
| Q6 Not always-rejected | YES — not DoS, not brute-force, not self-XSS. |
| Q7 Reasonable triager | PARTIALLY — injection primitive is real but impact is speculative without evidence of stored/rendered data. |

**Verdict: HOLD**
Reason: Injection primitive confirmed (accepts XSS strings, negative amounts, arbitrary eventType, CORS *), but no evidence of stored data being rendered to other users. Needs admin dashboard access to prove.

**Minimal read-only proof steps (partial):**
1. `curl -sS -D- -X POST "https://cable.betpanda.io/cable/user-event" -H "Content-Type: application/json" -d '{"eventType":"test","userId":"attacker123","referrer":"<script>alert(1)</script>","amount":-999}'` — confirm 200 "processed and saved".
2. Verify no auth requirement (no Cookie/Bearer header needed).

**Impact:** Medium (data poisoning) to High (if stored XSS confirmed in admin dashboard).
**CVSS 3.1:** 6.5 (AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:L/A:N) — no auth, but impact depends on server-side rendering.
**Reporting channel:** bugs.olivermaicher.eu

---

### LEAD 4: OAuth Redirect_URI Bypass on `betpanda.io/api/auth/authorize`

| Q | Answer |
|---|--------|
| Q1 Scope | YES — betpanda.io is in-scope. |
| Q2 Reachable | YES — was reachable. |
| Q3 Impact | N/A — REJECTED. |
| Q4 Provable | N/A — REJECTED. |
| Q5 Novel | N/A — REJECTED. |
| Q6 Not always-rejected | N/A — REJECTED. |
| Q7 Reasonable triager | N/A — REJECTED. |

**Verdict: INVALID**
Reason: False positive. `betpanda.io/api/auth/authorize` 301-redirects to `betpandacasino.io/api/auth/authorize` which returns SPA HTML (catch-all route). All `/api/*` paths on betpandacasino.io are SPA catch-all. No server-side OAuth endpoint exists. Confirmed by multiple probes returning 200 with HTML body.

---

### LEAD 5: k8s Dashboard Exposure on `dashboard.betpanda.io`

| Q | Answer |
|---|--------|
| Q1 Scope | YES — dashboard.betpanda.io is company-owned (CNAME to AWS ALB). |
| Q2 Reachable | NO — all probes timeout; ALB likely internal/private or auth-gated. |
| Q3 Impact | N/A — unreachable. |
| Q4 Provable | N/A — unreachable. |
| Q5 Novel | N/A — unreachable. |
| Q6 Not always-rejected | N/A — unreachable. |
| Q7 Reasonable triager | N/A — unreachable. |

**Verdict: INVALID**
Reason: ALB not publicly reachable (all HTTP probes timeout). CNAME pattern suggests k8s infrastructure but no evidence of exposure. Dead lead.

---

### LEAD 6: S3 Bucket Enumeration on `nano-public.s3.eu-west-1.amazonaws.com`

| Q | Answer |
|---|--------|
| Q1 Scope | YES — referenced by betpandacasino.io PWA manifest. |
| Q2 Reachable | YES — HTTP accessible. |
| Q3 Impact | NO — bucket listing returns 403 Forbidden; no enumeration. |
| Q4 Provable | YES — `GET /?list-type=2` returns 403 AccessDenied. |
| Q5 Novel | N/A — no finding. |
| Q6 Not always-rejected | N/A — no finding. |
| Q7 Reasonable triager | N/A — no finding. |

**Verdict: INVALID**
Reason: S3 bucket listing is disabled (403 Forbidden). Individual objects are accessible via known paths but no enumeration possible. Not a finding.

---

### LEAD 7: Strapi CMS `localStorage` baseURL Override

| Q | Answer |
|---|--------|
| Q1 Scope | YES — affiliates.betpanda.io integration. |
| Q2 Reachable | YES — JS bundle contains `localStorage.getItem("strapiApiUrlOverride")`. |
| Q3 Impact | PARTIALLY — if attacker can set localStorage (requires prior XSS or subdomain compromise), CMS content requests redirect to attacker server. But standalone impact is limited (content injection on affiliate pages, not player data). |
| Q4 Provable | PARTIALLY — localStorage override mechanism is visible in JS. But triggering requires XSS or subdomain control (out of passive scope). |
| Q5 Novel | PARTIALLY — localStorage-based API redirection is a known pattern but少见 in bug bounty. |
| Q6 Not always-rejected | YES — not on always-rejected list. |
| Q7 Reasonable triager | NO — standalone impact is medium at best; requires chaining with XSS. Not independently reportable. |

**Verdict: INVALID**
Reason: Requires prior XSS or subdomain compromise to exploit. Standalone impact is content injection on affiliate pages, not player data. Parked for chaining only; not independently reportable.

---

### LEAD 8: Flipt Feature-Flag Unauth Access on `flags.betpanda.io`

| Q | Answer |
|---|--------|
| Q1 Scope | YES — flags.betpanda.io is company-owned. |
| Q2 Reachable | NO — Cloudflare JS challenge (403) blocks all probes. |
| Q3 Impact | N/A — unreachable. |
| Q4 Provable | N/A — unreachable. |
| Q5 Novel | N/A — unreachable. |
| Q6 Not always-rejected | N/A — unreachable. |
| Q7 Reasonable triager | N/A — unreachable. |

**Verdict: INVALID**
Reason: Cloudflare JS challenge blocks all passive and active probes. Cannot assess without solving challenge. Dead lead.

---

### LEAD 9: IDOR on Affiliate Player Lookup by UID (`/rest/player/uid/{id}`)

| Q | Answer |
|---|--------|
| Q1 Scope | YES — affiliates.betpanda.io endpoint. |
| Q2 Reachable | YES — endpoint exists (confirmed from JS bundle + HTTP 400 on malformed requests). |
| Q3 Impact | YES (potential) — cross-affiliate player PII + financial data if server-side ACL missing. |
| Q4 Provable | NO — requires 2 affiliate accounts with players to demonstrate cross-tenant access. |
| Q5 Novel | UNKNOWN — classic BOLA pattern. |
| Q6 Not always-rejected | YES — IDOR on financial data not on always-rejected list. |
| Q7 Reasonable triager | PARTIALLY — endpoint existence confirmed but no evidence of ACL bypass. |

**Verdict: HOLD**
Reason: Sub-hypothesis of Lead 1 (CORS). The IDOR on `/rest/player/uid/{id}` is only exploitable if CORS+credentials works. Merge into Lead 1 report.

---

### LEAD 10: `betpanda.partners` CORS Misconfiguration

| Q | Answer |
|---|--------|
| Q1 Scope | YES — betpanda.partners is company-owned (dedicated host, same backend). |
| Q2 Reachable | YES — HTTP 200. |
| Q3 Impact | NO — CORS properly pinned (OPTIONS with evil Origin returns Vary:Origin but NO ACAO reflection). |
| Q4 Provable | YES — `curl -D- -X OPTIONS "https://betpanda.partners/rest/properties/manifest" -H "Origin: https://evil.com"` — no ACAO header. |
| Q5 Novel | N/A — no finding. |
| Q6 Not always-rejected | N/A — no finding. |
| Q7 Reasonable triager | N/A — no finding. |

**Verdict: INVALID**
Reason: CORS correctly restricts to own origin. OPTIONS and GET with evil Origin do NOT reflect origin. Not a finding.

---

### LEAD 11: Email Enumeration via `/rest/public/recover-password/email/{email}`

| Q | Answer |
|---|--------|
| Q1 Scope | YES — affiliates.betpanda.io endpoint. |
| Q2 Reachable | YES — HTTP accessible. |
| Q3 Impact | NO — explicitly listed as OUT of scope in scope.yml: "Lack of email address verification during account registration or account invitation", "Password and account recovery policies". |
| Q4 Provable | N/A — out of scope. |
| Q5 Novel | N/A — out of scope. |
| Q6 Not always-rejected | YES — username/email enumeration is on always-rejected list. |
| Q7 Reasonable triager | N/A — out of scope. |

**Verdict: INVALID**
Reason: Email enumeration is explicitly out of scope per program rules ("Username enumeration based on login or forgot password pages").

---

## SUMMARY TABLE

| # | Lead | Verdict | CVSS | Action |
|---|------|---------|------|--------|
| 1 | Wildcard CORS + credentials on affiliates.betpanda.io | **HOLD** | 9.1 | Needs auth-assisted POC |
| 2 | BOLA/IDOR on betpandacasino.io money-flow | **HOLD** | 8.8 | Needs 2 test accounts |
| 3 | Unauthenticated event injection on cable.betpanda.io | **HOLD** | 6.5 | Needs stored-XSS confirmation |
| 4 | OAuth redirect_uri bypass on betpanda.io | **INVALID** | — | False positive (SPA catch-all) |
| 5 | k8s dashboard exposure on dashboard.betpanda.io | **INVALID** | — | ALB unreachable |
| 6 | S3 bucket enumeration | **INVALID** | — | Listing disabled (403) |
| 7 | Strapi CMS localStorage override | **INVALID** | — | Requires XSS chain |
| 8 | Flipt feature-flag unauth | **INVALID** | — | CF blocked |
| 9 | IDOR on affiliate player UID lookup | **HOLD** | — | Merge into #1 |
| 10 | betpanda.partners CORS | **INVALID** | — | CORS properly pinned |
| 11 | Email enumeration | **INVALID** | — | Out of scope |

**Bottom line:** 3 HOLDs (all require auth-assisted testing), 8 INVALIDs. No VALID leads ready for immediate submission.
