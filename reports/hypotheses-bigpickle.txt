## 2026-09-18 22:22:10 UTC [target] (model bigpickle)
[HYP] Wildcard CORS+credentials on full /rest/* enables cross-origin ATO/exfil
class: MISCONFIG
asset: affiliates.betpanda.io/rest/*
confidence: 95
reasoning: Live this cycle — GET /rest/public/config 200 `ACAO:https://evil.example`+`ACAC:true` (398B byte-stable, operatorId=1); OPTIONS /rest/user/password/reset 200 same + allow-methods GET,POST,OPTIONS,PUT,HEAD,DELETE; allow-headers LACK Authorization ⇒ cookie/session crosses origin ungated; URL-bound reset code + set-2fa-setting + agent-limit tamper sit under same envelope; envelope byte-stable 18 consecutive days, bundle-agnostic.
evidence_needed: credentialed cross-origin GET returning non-401 readable body vs no-cookie 401 control.
verify_steps: HUMAN_ONLY — operator cred → POST /rest/public/login → GET /rest/user/metrics/affiliate Origin:https://evil.example + session cookie vs no-cookie 401 baseline.
impact: PII/commission/transaction exfil + password/2FA/agent-limit tamper → ATO — CRITICAL
testability: HUMAN_ONLY
[HYP] Cross-brand BOLA via shared App B (JWT aud/iss validation gap)
class: IDOR
asset: betpandacasino.io/rest/user/* + betpanda.partners/rest/user/*
confidence: 80
reasoning: Prior 13:26Z parity stands — settings 401 len=32 identical both hosts; casino ACAO own-host+ACAC:true vs partners NO ACAO but allow-headers incl Authorization (Bearer, curl-only); shared handler set + refresh OPTIONS parity unchanged.
evidence_needed: non-401 on partners using casino-minted JWT vs 401 control.
verify_steps: HUMAN_ONLY — casino login → POST /rest/user/refresh → GET https://betpanda.partners/rest/user/settings -H "Authorization: Bearer <jwt>".
impact: cross-brand wallet/balance/settings PII on real-money platform — CRITICAL
testability: HUMAN_ONLY
[HYP] Dual unauth cable ingestion into shared analytics/fraud pipeline
class: BUSLOGIC
asset: cable.betpanda.io + cable.betpandacasino.io/cable/user-event
confidence: 75
reasoning: Prior POST 200 confirmations stand (lax validator, arbitrary eventType/XSS/negative amounts); dual OPTIONS 204 ACAO:* baseline re-verified 08:49Z; 32-path read sweep 404; at passive ceiling.
evidence_needed: benign marker aggregated/reflected on owned reference account.
verify_steps: AUTH_HELPED — POST {"eventType":"verifybp23","userId":"verifybp23","registeredOn":"2026-09-18T20:00:00Z","amount":"0.01"} to one instance, check own dashboard.
impact: unauth event poisoning / fraud & bonus-pipeline write both hosts — MEDIUM
testability: AUTH_HELPED
[NEXT] HUMAN: request operator-issued sandbox/test affiliate credential via bugs.olivermaicher.eu (scope rule account_creation=restricted blocks live-prod register; every de-gate — POST /rest/public/login session, cross-brand Bearer handoff, cable marker reflection — is dead without a session; CT re-sweep this cycle adds zero surface).
[LEARN] ACCEPTED MISCONFIG @ affiliates.betpanda.io/rest/*: re-verified live this cycle — config 200 ACAO:https://evil.example+ACAC:true (398B byte-stable), password/reset OPTIONS same envelope + full methods, allow-headers lack Authorization; day 18, bundle-agnostic.
[LEARN] ACCEPTED SCAN @ crt.sh: fresh CT re-pull both zones returns only previously-assessed identities (retired stage/staging/cdn/worker + known live hosts); zero new subdomains, zero new attack surface — sweep re-closed with data.
[RISK] betpanda: 92 — three verified classes byte-stable into day 18 (flagship wildcard CORS+credentials, dual unauth write path, cross-brand 401 parity); CT re-pull confirms no new subdomain surface; all passive lanes exhausted with zero yield for 8+ consecutive cycles; impact-proof POC is exclusively gated on operator-issued credentials — autonomous progress nil, residual upside wholly human/operator-in-the-loop.
